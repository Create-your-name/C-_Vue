using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using webapi.Entity;

namespace webapi.Config
{
    public class TokenConfig : IEntityTypeConfiguration<BpmiToken>
    {
        public void Configure(EntityTypeBuilder<BpmiToken> builder)
        {
           /* builder.ToTable("bpmi_task");
            builder.Property(b => b.Taskid).HasMaxLength(128).IsUnicode().IsRequired();
            builder.HasOne<RxFormProductUnusual>(c => c.rxFormProductUnusual).WithMany(a => a.bpmiTokens).IsRequired();*/
        }
    }
}
